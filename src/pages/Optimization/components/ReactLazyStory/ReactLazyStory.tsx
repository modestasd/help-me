import React from 'react'

interface Props {
    
}

const ReactLazyStory = (props: Props) => {
    return (
        <div>
            ReactLazy
        </div>
    )
}

export default ReactLazyStory;
