import React from 'react'

interface Props {
    
}

const LocalizationSetup = (props: Props) => {
    return (
        <div>
            LocalizationSetup
        </div>
    )
}

export default LocalizationSetup
